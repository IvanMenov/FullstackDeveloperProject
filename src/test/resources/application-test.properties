# Disable scheduled tasks during integration tests to avoid background DB writes and external calls
spring.task.scheduling.enabled=false
# Explicitly disable product ingestion scheduler bean
app.ingestion.enabled=false

# Keep logs concise in tests
logging.level.com.respiroc.greg.fullstackdeveloperproject.service=INFO
